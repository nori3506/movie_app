import React from "react";
import Showcontainer from "../containers/ShowContainer";

const Showscreen = ({ route, navigation }) => {
  return <Showcontainer route={route} navigation={navigation} />;
};

export default Showscreen;
